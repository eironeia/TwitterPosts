# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterPosts' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterPosts

  target 'TwitterPostsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TwitterPostsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON'
  pod 'Kingfisher', '~> 4.0'
  pod 'TwitterKit'

end
